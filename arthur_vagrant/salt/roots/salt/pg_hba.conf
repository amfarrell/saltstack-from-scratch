# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust
# IPv4 local connections:
{% for host in pillar['database-access-allowed'] %}
host {{ pillar['database-name']}}  {{ pillar['database-user'] }}  {{ host }}  md5
{% endfor %}
